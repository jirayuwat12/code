#include "queue.h"

int main(){
  Q *f = (Q*)malloc(sizeof(Q));
  string inp;
  cin>>inp;
  switch (inp) {
    case "join"://en
      break;
    case "query"://request
      break;
    case "serve"://de
      break;
  }
}
