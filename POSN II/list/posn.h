#include <iostream>
#include <utility> //pair
#include <map> //map
#include <vector> //vector
#include <string.h> //string
#include <algorithm> //sort
#include <stdlib.h> //malloc
#include <conio.h> //getch

using namespace std;

/*
STL
    vector <T> name
    map <T,T> name
    pair <T,T> name
    memset : memset(pointer,value,amount)
    memcpy : memcpy(destination,original,amonut)
    malloc : #include <stdlib.h> : name = (T)malloc(sizeof(T)*amount); : free(name);
LIST
    Array-Based
      slowest
    Pointer-Base(Linked-list)

*/
